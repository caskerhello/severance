<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.third.severance.dao.IAdminDao">
    <select id="getAdmin" resultType="com.third.severance.dto.AdminVO" >
        select * from admin where adminid=#{param1}
    </select>

    <select id="getAllCount"  resultType="_int">
        select count(*) from ${param1} where ${param2} like concat('%', #{param3}, '%');
    </select>

    <select id="getMemberList"  resultType="com.third.severance.dto.AdminVO">
        select * from member where name like concat('%', #{param2}, '%') order by indate desc
            limit #{param1.displayRow} offset #{param1.startNum}
    </select>
</mapper>